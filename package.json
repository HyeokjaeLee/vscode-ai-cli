{
  "name": "vscode-ai-cli-monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "pnpm --filter webview-ui build && pnpm --filter ai-cli build",
    "dev:ui": "pnpm --filter webview-ui run watch"
  }
} 